include template

h3 Контакты (amoCRM)

table(border="1")
  tr
    th id
    th имя
    th action
  each contact in contacts
    tr 
      td= contact.id
      td= contact.name
      td
        a(href="/contacts/" + contact.id + '/chats') чаты

